# 更新日志

本项目遵循语义化版本规范。

## 3.5.1 - 2026-09-11

本次更新改善文章、随笔、媒体资源和恢复中心的日常操作体验，让列表更易读、编辑更连贯，并修复登录后刷新页面时登录窗口短暂闪现的问题。

### 随笔原位编辑

- 点击“编辑”后，编辑器直接在当前随笔卡片内展开，保留所在位置，不再跳到列表顶部。
- 在同一卡片中修改发布时间和 Markdown 内容，查看实时预览与字符数；保存后直接更新列表，避免整页重新加载造成跳动。
- 重新设计选择框、部分选中状态、日期输入、批量操作区和保存按钮，保留本机草稿与未保存离开提醒；保存期间继续输入的内容仍会保留。

### 更清楚的文章与媒体列表

- 文章标题和操作按钮使用更易读的字号，日期与字数移到标题下方，分类与标签集中显示；窄屏下改用卡片布局。
- 每篇文章保留“编辑”和“更多”入口。发布、取消发布、定时和删除等操作收进“更多”菜单，避免按钮挤成多排；定时时间在文章下方独立设置。
- “更多”使用矢量箭头，增加箭头旋转、菜单淡入下滑与悬停过渡；支持 Esc 和点击菜单外关闭，并遵循系统减少动态效果设置。
- 媒体卡片分开展示完整图片预览、文件名、大小、引用情况和操作按钮。引用说明可展开阅读，长路径可以换行，搜索与筛选更清晰。

### 恢复中心与登录体验

- 恢复中心重新安排备份占用、搜索、文件列表和操作区的间距，长文件名与保留策略不再挤在一起。
- 点击“预览恢复差异”后，在对应备份下方核对内容或图片；恢复操作显示进度并防止重复提交，列表读取失败时可直接重试。
- 登录后刷新页面时，先显示会话验证状态；有效会话直接进入当前页面，不再短暂显示登录窗口或其他页面。
- 上述页面同步适配亮色、暗色和手机宽度，并改善键盘焦点提示。

### 升级方法

在 Hexo 站点根目录运行 `npm install hexo-admin-panel@3.5.1`，然后重启 Hexo 并刷新管理页面。此次更新不需要修改配置或迁移内容，Node.js 仍要求 20 或更高版本。

## 3.5.0 - 2026-09-11

本次更新围绕“放心写作、明确发布、方便恢复”完善日常使用流程。新增内容历史、完整发布流程和统一恢复中心，支持随笔批量编写，并补齐媒体引用维护、配置编辑与单用户安全保护。

### 写作与内容恢复

- 文章和页面可以查看历史版本、比较差异并恢复旧内容。遇到其他窗口或外部修改时，会展示原版本、本地修改和服务器版本，帮助合并内容。
- 页面、随笔、About、配置、新建内容和菜单排序均支持本机草稿与离开提醒。断线或登录过期后可重新打开编辑器恢复；保存过程中继续输入的内容也会保留。
- 新增“恢复中心”，集中查看内容历史以及可识别的配置、随笔、菜单、分类和媒体备份。恢复前可查看文本差异或原图，并了解保留策略与磁盘占用。

### 随笔批量编写

- 在随笔列表勾选多条一起修改，或粘贴多篇文字批量添加；每批最多 100 条，可逐条预览 Markdown，也可统一设置时间。
- 粘贴内容使用单独一行 `---` 分隔；空段自动忽略，代码块内不拆分。移出批次不会删除已保存的随笔。
- 整批内容全部校验通过后才一次保存并备份，保留其他随笔及原有扩展字段。版本冲突时可核对服务器与本地差异，网络中断后也能识别已保存的新增条目。

### 发布前检查与线上确认

- 新增“保存版本确认 → 内容检查 → 构建 → 部署 → 线上验证”完整流程，每一步均记录状态、错误和对应的内容版本。
- 发布前检查缺图、常见站内死链、重复 permalink、缺标题、Front Matter 类型错误和代码块语言遗漏，并定位到文件、字段或行号。
- 只有线上发布标记及 HTML 页面与本次构建一致时，才显示验证通过。部署配置缺失或部署插件未安装时，会明确停在部署步骤。
- 修复待执行定时任务超过 200 条后被清理的问题；只裁剪已结束历史。发布日历与时间输入统一使用站点时区。

### 媒体与 Hexo 适配

- 媒体可查看反向引用；重命名前预览影响并同步更新已识别引用，删除前核对受影响内容。重命名失败时尝试恢复原文件与引用。
- 引用扫描增加站点和主题覆盖配置，避免把外部域名的同路径图片误认为本地引用，并明确显示扫描范围。
- 文章可集中管理公共图片及专属资源目录，支持上传、资源标签插入、重命名与删除影响预览；草稿发布时一并迁移资源目录。
- 适配 `post_asset_folder`、`asset_img`、文件名日期占位符、`:hash` 与自定义占位符，改善子路径站点和时区支持。

### 配置与单用户安全

- 配置保存前展示差异并校验字段，尽量保留原 YAML 注释；主题配置可区分默认值和覆盖值，通过删除覆盖恢复默认。
- 首次启动没有账号配置时，使用终端显示的随机一次性初始化凭据，替代固定的默认密码。
- 退出登录会在服务端撤销当前会话，并提供“退出所有会话”。密码验证改为异步并限制并发，降低登录操作对 Hexo 主线程的影响。
- 真实主题预览增加沙箱隔离，防止预览脚本读取后台登录信息；加强符号链接和 Windows junction 的文件边界校验。
- 文件已保存但 Hexo 刷新失败时，明确提示保存结果，避免重复提交。修复重建重启后脱离原终端的问题。

### 升级提示

- 运行环境仍要求 Node.js 20 或更高版本。升级后重启 Hexo，并刷新管理页面加载新版界面；插件仍采用单用户机制。
- 历史版本从启用后逐步积累，不会补齐此前所有修改。每个文本文件最多保留 50 版、二进制文件 5 版，总上限为 10000 版 / 512 MiB；旧备份按各自策略保留。
- 本机草稿保存在当前浏览器中；请勿在恢复前清理浏览器站点数据。备份与登录状态所在的 `.hexo-admin/` 应继续排除在 Git 和公开站点之外。
- 启用文章资源目录时，`new_post_name` 需以 `.md` 结尾。配置文件保存后需重启 Hexo，才能按新配置运行完整发布流程。
- 线上验证最多检查 2000 个 HTML 页面，每页不超过 10 MiB。CDN 延迟、HTML 改写或重定向可能导致部署完成但验证未通过；可查看步骤记录后重试。静态检查不覆盖所有动态模板引用或外部链接可用性。

详细操作见 [内容与发布工作流](docs/workflows.md)。

## 3.3.0 - 2026-09-04

3.3.0 将管理后台从“文章编辑器”扩展为完整的 Hexo 内容与发布工作台。本次更新重点覆盖独立页面、真实主题预览、发布任务、分类标签和内容模板，并完善项目标识与开源信息。

### 页面与内容工作流

- 新增通用页面管理：可以直接创建、编辑和回收 `source/**/index.md` 以及 `source/` 下的其他独立 Markdown 页面，不再只支持 About 页面。
- 页面创建支持选择 Hexo scaffold、设置 layout 和源码路径；常见主题的 `navbar.links` 或 `menu` 还可以在后台调整显示顺序。
- 新建文章现在直接读取站点已有的 `scaffolds/*.md`，可选择草稿、未完成、待审核、计划中或立即发布。
- 新增分类标签中心，可查看已发布文章与草稿中的使用次数，并安全地合并、重命名或删除分类标签。
- 文章编辑器中的分类和标签输入升级为可搜索、可创建的选择器，管理大量分类时更容易定位。

### 真实主题预览

- 文章和独立页面均可生成真实主题预览，不再局限于 Markdown 渲染和模拟页面外壳。
- 预览使用当前站点的 Hexo 配置、主题、插件和最终 permalink 临时构建，并直接在同源 iframe 中展示。
- 预览链接使用随机 token，默认 30 分钟后失效；临时文件会在服务重启时清理。

### 发布中心

- 新增发布任务月历，在一个页面集中查看待发布、等待重试、已完成、失败和已取消的计划。
- 定时发布失败后可按任务配置自动重试，也可以立即手动重试；服务重启后会恢复未完成任务和历史记录。
- 生成、部署、清理和重建任务现在展示实时阶段、百分比进度与 Hexo 原始输出日志。
- 运行中的命令支持取消，失败或已取消任务可以重新执行，并保留任务来源和历史结果。

### 项目信息与文档

- 新增“关于插件”页面，集中展示插件能力、运行版本、设计原则、GitHub、npm、许可证和第三方开源引用。
- 新增原创简约 Logo，并统一用于侧栏、登录页、浏览器标题与项目 README；产品名称统一为 `Hexo Admin Panel`。
- 面向 GitHub 发布重写中文 README，并提供内容完整对应的英文 README；两种语言可在文档顶部双向切换。
- 新增 `THIRD_PARTY_NOTICES.md`，逐项列明 Hexo、hexo-front-matter、js-yaml、sharp、Vue、marked 与 DOMPurify 的用途、版权、许可证和源码地址。

### 升级提示

- 插件运行环境要求 Node.js 20 或更高版本；从 npm 当前公开的 3.1.0 升级时请先确认运行时版本。
- 请继续将 `.hexo-admin/` 和首次初始化使用的 `_admin-config.yml` 排除在 Git 与公开站点之外。
- 定时发布依赖 Hexo 服务持续运行；主题菜单保存后需要重新构建或重启 Hexo 才会生效。
- 真实主题预览会执行站点当前安装的生成器、渲染器和插件，只应在可信的 Hexo 项目中使用。

## 3.2.0 - 2026-09-04

### 可靠性与安全

- 将非默认明文管理员密码自动迁移为私有状态文件中的 PBKDF2 哈希，迁移时轮换 JWT 密钥，并从独立 `_admin-config.yml` 清除凭据字段。
- 为管理端静态资源和 API 增加 CSP、禁止嵌入、MIME 嗅探防护、Referrer Policy 与 Permissions Policy 等安全响应头。
- Node.js 运行时要求统一调整为 20 或更高版本，与 Sharp 生产依赖保持一致。

### 内容与媒体

- 媒体库递归管理 `source/images` 子目录，以完整相对路径区分同名资源并分析嵌套引用。
- 文章与 About 的 Front Matter 编辑器增加显式类型选择，避免 `"true"`、`"123"` 等字符串在保存后意外改变类型。
- 批量发布、撤回和删除完成全部文件操作后仅执行一次 Hexo source refresh。

### 命令任务

- 生成、部署、清理和重建改为可查询的后台任务，持久化任务状态、阶段日志、结果与错误信息。
- 工作概览展示最近命令任务的实时状态与日志；服务重启后会把未完成任务标记为中断失败。

### 内容工作流

- 新文章默认创建为真正的 Hexo 草稿，发布和撤回会在 `source/_drafts` 与 `source/_posts` 之间安全移动文件。
- 文章编辑器增加未保存离开保护、本地自动保存和恢复提示。
- 媒体搜索改为服务端全库搜索，分页结果和总数保持一致。
- 站点与主题配置增加 revision 并发冲突检测，保存和恢复不再静默覆盖外部修改。
- 文章搜索扩展到正文、分类、标签、路径和 Front Matter，并支持多关键词匹配。
- 文章列表增加带 revision 预检的批量发布、撤回和删除。
- 草稿增加持久化定时发布任务，到期后自动迁移到 `_posts`，失败状态可取消或重新安排。
- 媒体库增加引用来源分析、已使用/未使用筛选，以及带原图备份的 JPEG、PNG、WebP 压缩。

### 工程质量

- 增加 ESLint、Node/Vue 覆盖率命令和最低前端覆盖率阈值。
- 增加 Node.js 20/22 GitHub Actions 流程，自动执行 lint、测试、覆盖率、构建和 npm 打包检查。
- 修复“重新构建并重启”可能误判旧实例仍在线的问题；旧服务现在会优雅停止 watcher 并刷新 Hexo 状态，辅助进程确认创建和新实例监听后才算重启完成。

## 3.1.0 - 2026-08-28

### 国际化

- 管理后台全面支持简体中文与 English 切换，覆盖导航、页面、编辑器、弹窗、提示、空状态和常用错误信息。
- 语言选择保存在浏览器中，并同步页面语言标记；桌面、登录页和移动端均可切换。
- Redefine 主题字段名称、说明和分区改为内置中英双语元数据，打开配置页不再解析 YAML 注释。

### 文档

- 重写 README 的项目说明与快速开始结构，移除冗长更新清单和重复功能描述。

## 3.0.0 - 2026-08-26

### 管理体验

- 管理后台采用更现代的 Vue 3 工作台视觉，页面切换增加过渡动画。
- 增加持久化亮色/暗色双模式，补齐表格、列表、弹窗、认证页、编辑器和主题预览的暗色适配。
- “说说管理”更名为“随笔管理”，导航和页面文案统一更新。

### 编辑器

- 文章编辑支持分栏、仅 Markdown、仅预览和完整源码模式。
- Markdown 编辑区支持源码高亮，预览调用 Hexo 渲染并保留安全清理。
- 支持文章首图 `cover` 字段上传、预览、移除和保存。
- 新增 About 关于页面编辑，支持 `source/about/index.md` 的 Front Matter、Markdown、源码和主题预览。

### 资源与配置

- 媒体资源支持安全重命名，禁止修改扩展名并防止同名覆盖。
- Redefine 主题配置优先读取 YAML 中文注释生成字段名称和说明。
- 配置项支持键盘可访问的悬浮用途提示，颜色配置支持 Hex/RGB(A)/HSL(A) 色块预览。
- 站点或主题配置保存、恢复后自动执行清理、生成和 Hexo 服务重启，管理页自动等待恢复。

### 质量

- 增加 About API、媒体重命名、暗色模式和 Redefine 配置元数据测试。
- npm 发布前继续自动执行全部后端测试、Vue 测试和生产构建。

## 2.0.0 - 2026-08-26

### 架构

- 后端按 `auth`、`posts`、`essays`、`media`、`config`、`themes`、`commands` 和 `trash` 功能模块拆分。
- 引入统一异步路由包装、身份认证、JSON 响应和错误边界。
- 引入受 Hexo 根目录约束的文件 Repository、原子写入和统一输入验证。
- 将 Front Matter、YAML、路径、revision 和主题解析拆分为可独立测试的模块。
- 管理端迁移至 Vue 3 + Vite，并拆分为页面、布局、组件、composable 和工具模块。

### 安全与数据保护

- 修复异步接口异常可能导致 Hexo 服务崩溃的问题。
- 修复文章发布操作重写并破坏未知 Front Matter 字段的问题。
- Markdown 预览改用 DOMPurify 清理，阻止脚本和事件属性注入。
- 升级 `js-yaml`，并统一 YAML 编解码入口。
- 默认 `admin/admin` 账号改为一次性凭据，首次登录强制设置新密码。
- 增加登录失败限流、弱配置迁移与启动配置校验。
- 文章和说说写入增加 revision 并发冲突检测。
- 增加文章、媒体回收站以及站点、主题、说说配置备份。
- 上传增加数量、请求大小、扩展名、MIME 和文件签名校验，强制禁用 SVG。

### 功能

- 文章编辑器支持可视化与完整 Markdown/Front Matter 源码模式同步。
- Redefine 主题由后端 schema 作为字段、标签、分区和版本兼容信息的唯一来源。
- 支持从主题目录、npm 主题包读取配置，并安全写入站点覆盖文件。
- 增加 Redefine `source/_data/essays.yml` 说说的创建、编辑、删除和安全预览。
- 增加 `hexo clean` + `hexo generate` 一键重新构建。
- 增加独立进程交接重启 Hexo server，并在管理端自动等待服务恢复。

### 开发与质量

- 增加 Vite 热更新、本地 `file:` 依赖开发流程和生产静态资源构建。
- 增加完整 API、纯函数、Vue 组件、XSS、并发冲突、备份和打包测试。
- npm 发布前自动执行全部测试和生产构建。

### 升级提示

- 前端源码开发与构建需要 Node.js 20.19+；npm 包中的预构建后台不需要 Vite。
- 使用默认账号的站点会在首次登录后强制修改密码。
- `.hexo-admin/` 包含凭据状态、回收站与备份，必须排除在公开部署和版本控制之外。
- 一键重启针对直接运行 `hexo server` 的环境；新进程日志位于 `.hexo-admin/restart.log`。
